Import the "jkg" function into your component or service by adding the following code:


> import { jkg } from 'my-lib';


Call the "jkg" function with a string or a number as input and store the result in a variable, like this:


> const reversed = jkg('hello');
> console.log(reversed); // Output: olleh

> const reversedNumber = jkg(1234);
> console.log(reversedNumber); // Output: 4321
